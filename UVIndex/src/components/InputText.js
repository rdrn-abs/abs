import React from 'react';

const InputText = (context) =>
	<div>
		<h4>SEARCH LOCAL UV INDEX </h4>
		<input
			className="uv-search"
			type="text"
			placeholder="Enter the value"
		/>
		<button>Submit</button>
	</div>
	;

export default InputText;
